#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];

    int k = 1;                        
    for (int i = 1; i < n; i++) {
        if (a[i] != a[k - 1]) {    
            a[k] = a[i];
            k++;
        }
    }
    for (int i = 0; i < k; ++i) {
        cout << a[i]<<" ";
    }
    return 0;
}
