#include <iostream>
#include <vector>
using namespace std;
int main() {
    int n; cin>>n;
    vector<int> a(n);
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    bool sort=true;
    for(int i=0;i<n-1;i++){
        if(a[i]>a[i+1]){
            sort=false; break;
        }
    }
    if(sort=true){
        cout<<"Sorted";
    }
    else{
        cout<<"Not sorted";
    }
    return 0;
}
