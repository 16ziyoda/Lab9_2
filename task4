#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; ++i) cin >> a[i];

    int uniq = 0;
    for (int i = 0; i < n; ++i) {
        bool seen = false;
        for (int j = 0; j < i; ++j) {
            if (a[j] == a[i]) { seen = true; break; }
        }
        if (!seen) ++uniq;
    }

    cout << "Unique=" << uniq << '\n';
    return 0;
}
